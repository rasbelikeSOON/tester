name: PR Review via Reusable Workflow

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  review:
    uses: rasbelikeSOON/prbotfinal/.github/workflows/groq-pr-review.yml@main
    secrets:
      GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
