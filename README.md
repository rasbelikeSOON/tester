# Random Password Generator

A simple Python tool to generate random passwords.

## Features
- Custom password length
- Option to include/exclude special characters

## Usage
Run the script with Python:
```bash
python random_password.py --length 16
```
To exclude special characters:
```bash
python random_password.py --no-special
```
